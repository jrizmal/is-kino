@model IEnumerable<web.Models.GenreMovie>

@{
    ViewData["Title"] = "Genres";
}

<h1>Genres</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Movie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genre.GenreName)
            </td>
            <td>
                @* <a asp-action="Edit" asp-route-id="@item.GenreMovieID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GenreMovieID">Details</a> | *@
                <a asp-action="Delete" asp-route-id="@item.GenreMovieID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
