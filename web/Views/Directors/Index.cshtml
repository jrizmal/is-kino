@model IEnumerable<web.Models.Directors>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Movie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.People)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Movie.MovieId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.People.PeopleID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DirectorsID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DirectorsID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DirectorsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
